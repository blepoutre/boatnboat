
<div class="d-flex container w-50 justify-content-center flex-column mt-5">
  <div class="card-trip">
   <img src= <%= @booking.boat.picture_url %> />
   <div class="card-product-infos">
   <div class="ms-5 mb-2">
    <h1 class="mt-3"><%= @booking.boat.name %></h1>
    <h5>Votre reservation </h5>
      <li>Date d'arrivée <%= @booking.begin_date %></li>
      <li>Date de fin <%= @booking.end_date %></li>
   </div>
  <div class="d-flex justify-content-end">
  <p id="cards-status">Statut: <%= @booking.status %></p>
  <a class="btn btn-flat" type="submit" href="/boats">Valider</a>
  </div>
  </div>
  </div>
</div>
</div>
</div>
